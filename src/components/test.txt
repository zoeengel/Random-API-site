const Funny = () =>  {
  const [content, setContent] = useState([]);

  const getFunny = () => {
    fetch('https://v2.jokeapi.dev/joke/pun?type=twopart')
    .then((res) => res.json())
    .then(data => {
      console.log(data);
      setContent(data);
    })
  }
  
  useEffect(() => {
    fetchJokes();
  }, [])

  return (
    <p>{content.setup}</p>
  );
}